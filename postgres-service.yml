apiVersion: v1 
kind: Service

metadata:
  name: postgres
  labels:
    app: voting-app
    name: postgres-service

spec: 
  selector:
    app: voting-app
    name: postgres
  ports:
    - port: 5432
      targetPort: 5432
      